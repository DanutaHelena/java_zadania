package zadania.zadaniedomowe;

import java.awt.*;
import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.List;
import java.util.concurrent.ThreadLocalRandom;

public class ZadanieMaxMin {

 /*   public static void main(String[] args) {
        int rozmiar = Integer.parseInt(args[0]);
        int zakres = Integer.parseInt(args[1]);

        int[] tablica = new int[rozmiar];

        for (int i = 0; i < tablica.length; i++)
            tablica[i] = ThreadLocalRandom.current().nextInt(1, zakres + 1);

        int max = tablica[0];
        int min = tablica[0];

        for (int i = 0; i < tablica.length; i++)
            System.out.println((i + 1) + " : " + tablica[i]);


        for (int i = 0; i < tablica.length; i++) {
            if (tablica[i] > max)
                max = tablica[i];

            if (tablica[i] < min)
                min = tablica[i];

        }

        System.out.println("Maksymalna liczba w tablicy to: " + max);
        System.out.println("Minimalna liczba w tablicy to: " + min);
    }

  */
}